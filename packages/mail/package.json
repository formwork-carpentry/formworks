{
  "name": "@carpentry/mail",
  "version": "1.0.0-alpha.0",
  "description": "Mail manager with pluggable adapters (SMTP, SES, Mailgun, Resend)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "biome check src/"
  },
  "dependencies": {
    "@carpentry/core": "workspace:*"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "private": true
}
