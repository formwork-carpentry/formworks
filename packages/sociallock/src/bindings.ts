/**
 * @module @carpentry/sociallock/bindings
 */

export const SOCIALLOCK_BINDINGS = {
  service: "sociallock.service",
  controller: "sociallock.controller",
  stateStore: "sociallock.state-store",
  providers: "sociallock.providers",
  userRepository: "sociallock.user-repository",
} as const;
