/**
 * @carpentry/formwork — The Carpenter Framework
 *
 * This is the umbrella package for the Carpenter framework.
 * Install this single package to get the entire framework.
 *
 * Usage:
 *   npm install @carpentry/formwork
 *
 * Then import from the individual modules:
 *   import { bootstrap } from '@formwork/foundation';
 *   import { Router, HttpKernel, serve } from '@formwork/http';
 *   import { Container } from '@formwork/core/container';
 */

export const VERSION = '1.0.0-alpha.0';
export const FRAMEWORK = 'Carpenter';
