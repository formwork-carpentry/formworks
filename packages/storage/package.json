{
  "name": "@carpentry/storage",
  "version": "1.0.0-alpha.0",
  "description": "Storage manager with pluggable adapters (Local, S3, GCS, Azure)",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "biome check src/"
  },
  "dependencies": {
    "@carpentry/core": "workspace:*",
    "@carpentry/storage-s3": "workspace:*"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "private": true
}
