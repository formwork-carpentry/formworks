{
  "name": "@carpentry/realtime",
  "version": "1.0.0-alpha.0",
  "description": "Real-time: presence, CRDT collaboration, live cursors",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./*": "./src/*.ts"
  },
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist",
    "lint": "biome check src/"
  },
  "license": "MIT",
  "files": [
    "dist"
  ],
  "peerDependencies": {
    "@carpentry/formworks": ">=1.0.0"
  },
  "devDependencies": {
    "@carpentry/formworks": "workspace:*"
  }
}
